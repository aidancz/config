to tangle:
M-x org-babel-tangle (C-c C-v C-t)

#+begin_src emacs-lisp :tangle ~/.emacs.d/early-init.el
(setq inhibit-x-resources t)
(load-theme 'wombat t)
#+end_src

#+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
(set-face-attribute 'default nil :height 140)
#+end_src
