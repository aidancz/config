# "a <- b" means a is linked by b
# "a << b" means a is sourced by b

.config/foo <- ~/.config/foo
# foo position is used by foo

.config/.foo <- ~/.config/.foo
# .foo position is not used by foo

	.sh
	profile <- ~/.zprofile

	.x11
	xprofile <- ~/.xprofile
	xresources << xprofile
	xinitrc <- $XINITRC

